Runs where a single bash call was used for the entire batch of runs (i.e. the .jl file took care of changing the test-case)

Here, we use an offset of -0.25 rather than 0.75, so that the attacker doesn't get an 'extra time unit' to escape (remember the attacker arrives at nodes at half-integer times, including arriving at v_infty).